<UserControl x:Class="SlimsteMens.Wpf.Views.Controls.ScoreboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ItemsControl ItemsSource="{Binding Teams}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="{Binding Name}" Width="150" FontSize="24" />
                    <ProgressBar Minimum="0" Maximum="60" Value="{Binding Seconds}" Width="200" Margin="10,0" />
                    <TextBlock Text="{Binding Seconds}" Width="50" FontSize="24" />
                </StackPanel>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
